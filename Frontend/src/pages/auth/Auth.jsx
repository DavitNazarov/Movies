import { AuthForm } from "@/components/auth/auth-form";
import React from "react";

const LogIn = () => {
  return <AuthForm className="flex justify-center items-center" />;
};

export default LogIn;
